[project]
name = "low-carbon-building-optimisation"
version = "0.1.0"
description = "Decision-support model for low-carbon building electricity strategy analysis"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
